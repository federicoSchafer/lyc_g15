// ===============================
// Declaraciones de variables
// ===============================
Integer a;
Integer b;
Integer c;
Boolean y;
Boolean z;
DateConverted dateC;

/* Variables inicializadas */
a := 2;
b := 3;
c := 5;

// ===============================
// Expresiones aritméticas
// ===============================
a := b + c * (a - 1);
b := (a + b) / 2;
c := a * b - (c + 10);

// ===============================
// Expresiones booleanas
// ===============================
y := equalExpressions(a + b, 5, b * 2, 3 + 2, a + b);   // true
y := equalExpressions(0, -5, a + b);                   // false
z := (a > b) and not (b < c) or (a := c);

// ===============================
// Conversión de fechas
// ===============================
dateC := convDate(21-08-2025);

// ===============================
// Sentencias de control
// ===============================

if (a < b) {
    a := b;
}

while (c > 0) {
    c := c - 1;
}

// ===============================
// Comentarios
// ===============================

// Comentario de línea
/* Comentario
   multilinea */

// ===============================
// Casos de error comentados
// ===============================

// 1234          -> ERROR: no puede iniciar una sentencia con una constante
// Integer var1; -> ERROR: identificador demasiado largo o inválido si excede el límite
// a := ;         -> ERROR: falta expresión a la derecha del asignador
// convDate(21/08/2025); -> ERROR: separador de fecha incorrecto, debe ser '-'
